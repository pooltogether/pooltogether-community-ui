/// <reference types="react" />
/**
 * TODO: Make settings extendible for all apps
 * @param {*} props
 * @returns
 */
export declare function SettingsContainer(props: any): JSX.Element;
export declare namespace SettingsContainer {
    var defaultProps: {
        sizeClassName: string;
        className: string;
    };
}
